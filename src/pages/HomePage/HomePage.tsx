import { FC } from 'react'
import { Helmet } from 'react-helmet'

const HomePage: FC = ({}) => {
    return (
        <>
            <Helmet>
                <title>Главная</title>
            </Helmet>
            <h1>Главная</h1>
        </>
    )
}

export default HomePage
